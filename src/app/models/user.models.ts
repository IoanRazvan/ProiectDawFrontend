export interface DirectSignInUser {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
}

export interface DirectLogInUser {
    email: string;
    password: string;
}